/**
 *  |- 布局
 *  |- mk.layout.js
 *  |- Layout
 *  |- 参数： 
 *    |- top: 弹出层相距页面顶部距离，默认为100px
 *    |- width: 弹出层宽度，默认为500px
 *    |- height: 弹出层高度，默认为500px
 *    |- url: 弹出层欲加载的页面地址，默认为空
 *    |- auto_hide: 是否当鼠标离开自动隐藏
 *  |- 依赖： 
 *    |- Parser: mk.parser.js
 *  |- 2012-08-08
 *  |- Author: <a mailto="hegq@neusoft.com">Puras.He</a>
 */
(function( $, undefined ) {

  $.fn.mk_layout = function(options) {};

  $.fn.mk_layout.parse_options = function(target) {};

  $.fn.mk_layout.methods = {};

  $.fn.mk_layout.defaults = {};
})( jQuery );