(function( $, undefined ) {
	var button_default_options = {
		message: 'MK Panel',
		panel_class: 'mk-panel',
		panel_header_class: 'mk-panel-header',
		panel_content_class: 'mk-panel-content',
		pane_action_bar_class: 'mk-panel-action-bar'
	};
	$.fn.mk_button = function(options) {
		var opts = $.extend({}, button_default_options, options || {});
		var self = this;
		
		this.addClass('button');
	};
})( jQuery );