package com.neusoft.clw.yw.xs.poimanage.service;

import java.util.List;

import com.neusoft.clw.common.exceptions.BusinessException;
import com.neusoft.clw.common.service.Service;
import com.neusoft.clw.yw.xs.poimanage.ds.PoiInfo;

/**
 * 服务点管理service接口
 * @author JinPeng
 */
public interface PoiManageService extends Service {
    // 导入服务点数据
    void importPoiInfos(List < PoiInfo > list) throws BusinessException;
}
